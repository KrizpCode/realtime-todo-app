type Todo {
    id: ID!
    title: String!
    completed: Boolean!
}

type Mutation {
    addTodo(title: String!): Todo
    removeTodo(id: ID!): Boolean
    updateTodo(id: ID!): Todo
}

type Query {
    todos: [Todo!]!
}

type Subscription {
    todos: [Todo!]!
}
